// @ts-nocheck
import { Download, Filter, Search, X } from "lucide-react";

export function FilterPanel({ resetFilters, onExport, filtered, query, setQuery, country, setCountry, city, setCity, material, setMaterial, primaryFunction, setPrimaryFunction, countryOptions, cityOptions, materialOptions, functionOptions, minHeight, maxHeight, setMinHeight, setMaxHeight, minYear, maxYear, setMinYear, setMaxYear }) {
  return <section className="rounded-3xl border border-slate-200 bg-white p-4 shadow-sm md:p-5"><div className="mb-4 flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div className="flex items-center gap-2 text-sm font-medium text-slate-700"><Filter size={16} />Filters</div><div className="flex flex-wrap gap-2"><button onClick={resetFilters} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-sm text-slate-700 transition hover:bg-slate-50"><X size={15} /> Reset</button><button onClick={() => onExport(filtered)} className="inline-flex items-center gap-2 rounded-xl bg-slate-950 px-3 py-2 text-sm font-medium text-white transition hover:bg-slate-800"><Download size={15} /> Export CSV</button></div></div><div className="grid gap-4 lg:grid-cols-[1.3fr_repeat(4,1fr)]"><label className="relative block"><Search className="absolute left-3 top-3 text-slate-400" size={18} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search building, city, country, material, or function" className="w-full rounded-2xl border border-slate-200 bg-slate-50 py-3 pl-10 pr-4 text-sm outline-none transition focus:border-slate-400 focus:bg-white" /></label><select value={country} onChange={(event) => { setCountry(event.target.value); setCity("All"); }} className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none transition focus:border-slate-400 focus:bg-white">{countryOptions.map((option) => <option key={option}>{option}</option>)}</select><select value={city} onChange={(event) => setCity(event.target.value)} className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none transition focus:border-slate-400 focus:bg-white">{cityOptions.map((option) => <option key={option}>{option}</option>)}</select><select value={material} onChange={(event) => setMaterial(event.target.value)} className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none transition focus:border-slate-400 focus:bg-white">{materialOptions.map((option) => <option key={option}>{option}</option>)}</select><select value={primaryFunction} onChange={(event) => setPrimaryFunction(event.target.value)} className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none transition focus:border-slate-400 focus:bg-white">{functionOptions.map((option) => <option key={option}>{option}</option>)}</select></div><div className="mt-4 grid gap-4 md:grid-cols-2"><div className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3"><div className="mb-2 flex justify-between text-xs font-medium text-slate-500"><span>Height range</span><span>{minHeight} m to {maxHeight} m</span></div><div className="grid gap-3 md:grid-cols-2"><input type="range" min="246" max="828" value={minHeight} onChange={(event) => setMinHeight(Math.min(Number(event.target.value), maxHeight))} className="w-full accent-slate-950" /><input type="range" min="246" max="828" value={maxHeight} onChange={(event) => setMaxHeight(Math.max(Number(event.target.value), minHeight))} className="w-full accent-slate-950" /></div></div><div className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3"><div className="mb-2 flex justify-between text-xs font-medium text-slate-500"><span>Completion year range</span><span>{minYear} to {maxYear}</span></div><div className="grid gap-3 md:grid-cols-2"><input type="range" min="1931" max="2023" value={minYear} onChange={(event) => setMinYear(Math.min(Number(event.target.value), maxYear))} className="w-full accent-slate-950" /><input type="range" min="1931" max="2023" value={maxYear} onChange={(event) => setMaxYear(Math.max(Number(event.target.value), minYear))} className="w-full accent-slate-950" /></div></div></div></section>;
}
