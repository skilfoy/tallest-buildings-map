// @ts-nocheck
import { MapPin } from "lucide-react";

export function ClusterPanel({ selectedGroup, selectedBuilding }) {
  return <><section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center gap-3"><div className="rounded-2xl bg-slate-950 p-3 text-white"><MapPin size={20} /></div><div><p className="text-sm text-slate-500">Selected cluster</p><h2 className="text-xl font-semibold">{selectedGroup?.location ?? "No results"}</h2></div></div>{selectedGroup && selectedBuilding && (<div className="space-y-3 text-sm"><div className="grid grid-cols-2 gap-3"><div className="rounded-2xl bg-slate-50 p-3"><p className="text-slate-500">Buildings</p><p className="font-semibold">{selectedGroup.count}</p></div><div className="rounded-2xl bg-slate-50 p-3"><p className="text-slate-500">Tallest</p><p className="font-semibold">{selectedBuilding.heightM} m</p></div></div><div className="rounded-2xl bg-slate-50 p-3"><p className="text-slate-500">Tallest building</p><p className="font-semibold">#{selectedBuilding.rank} {selectedBuilding.name}</p></div><div className="rounded-2xl bg-slate-50 p-3"><p className="text-slate-500">Coordinate precision</p><p className="font-semibold">City-level, shared by cluster</p></div></div>)}</section><section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="mb-4 text-lg font-semibold">Cluster records</h2><div className="max-h-[360px] space-y-2 overflow-auto pr-1">{(selectedGroup?.buildings ?? []).slice().sort((a, b) => a.rank - b.rank).map((row) => (<div key={row.rank} className="rounded-2xl border border-slate-200 bg-white p-3"><div className="flex items-start justify-between gap-3"><div><p className="font-medium">#{row.rank} {row.name}</p><p className="mt-1 text-sm text-slate-500">{row.function}</p></div><span className="rounded-full bg-slate-100 px-2 py-1 text-xs font-medium text-slate-700">{row.heightM} m</span></div></div>))}</div></section></>;
}
